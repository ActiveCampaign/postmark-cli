export const command = 't'
export const desc = ''
export const builder = {}
export const handler = (): void => exec()

/**
 * Execute the command
 */
const exec = (): void => {
  console.log('hai')
}
